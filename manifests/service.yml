apiVersion: v1
kind: Service
metadata:
    name: whitespacemxgsspringbootdocker
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: whitespacemxgsspringbootdocker